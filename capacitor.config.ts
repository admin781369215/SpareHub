import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sparehub.app',
  appName: 'SpareHub',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
